name: Makefile
on:
  push:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: install deps
        run: |
          sudo apt install fonttools fontforge python3-fontforge
          npm install
      - name: build ttf
        run: |
          make
      - uses: svenstaro/upload-release-action@master
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: "./build/Twemoji Mozilla.ttf"
          asset_name: twemoji-colr.ttf
          tag: ${{ github.ref }}
          overwrite: true
